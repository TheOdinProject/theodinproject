<%= render ModalComponent.new(title: 'Submit your project')  do %>
  <%= render 'project_submissions/form', project_submission: @project_submission, model: [@lesson, @project_submission] %>
<% end %>
