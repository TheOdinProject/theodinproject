<%= title("Notifications") %>

<div class="page-container h-screen">
  <h1 class="page-heading-title">Notifications</h1>
  <ul class="space-y-3">
    <% unless @notifications.empty? %>
      <%= render NotificationComponent.with_collection(@notifications) %>
    <% else %>
      <div class="text-center mx-auto">
        <h3 class="text-lg leading-6 font-medium text-slate-900 dark:text-slate-300 mb-2">No notifications yet</h3>
        <p class="text-slate-500 dark:text-slate-400">Why not take your next lesson while you wait?</p>
      </div>
    <% end %>
  </ul>
</div>
